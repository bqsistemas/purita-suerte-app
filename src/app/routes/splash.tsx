import React from 'react';
import { View } from 'react-native';

/**
 * Splash screen shown while restoring session.
 */
export default function SplashScreen() {
  return <View style={{ flex: 1, backgroundColor: '#7e22ce' }} />;
}
